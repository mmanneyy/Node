const bf = Buffer.from("hello world");
console.log(bf);
console.log(bf.toString());
//buffer stores binary data. It's displayed in hexadecimal 
// format in order to be able to store more data and use less space for that 